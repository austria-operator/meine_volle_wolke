apiVersion: v1
kind: Service
metadata:
  name: {{ include "image_uploader.fullname" . }}
  labels:
    {{- include "image_uploader.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "image_uploader.selectorLabels" . | nindent 4 }}
